import { ProjectbyidPipe } from './projectbyid.pipe';

describe('ProjectbyidPipe', () => {
  it('create an instance', () => {
    const pipe = new ProjectbyidPipe();
    expect(pipe).toBeTruthy();
  });
});
